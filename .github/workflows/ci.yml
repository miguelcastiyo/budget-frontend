name: Frontend CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Check out canonical parity fixtures
        uses: actions/checkout@v4
        with:
          repository: miguelcastiyo/budget-backend
          path: budget-backend

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Fast deterministic tests
        env:
          BUDGET_BACKEND_FIXTURE_ROOT: ${{ github.workspace }}/budget-backend/tests/fixtures/privacy-parity
        run: npm test

      - name: Build
        env:
          BACKEND_ORIGIN: https://api-budget.miguelcastillo.info
          NEXT_PUBLIC_GOOGLE_CLIENT_ID: ci-placeholder.apps.googleusercontent.com
        run: npm run build
